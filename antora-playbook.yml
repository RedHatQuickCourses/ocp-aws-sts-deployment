site:
  title: Deploy Self-Managed Openshift on AWS with STS
  start_page: ocp-aws-sts-deployment::index.adoc

content:
  sources:
  - url: ./

asciidoc:
  attributes:
    page-pagination: true

ui:
  bundle:
    url: ./ui-bundle/ui-bundle.zip
  supplemental_files: ./supplemental-ui
